# xtask

Run `cargo xtask fast` for formatting, architecture, Phase 0 drift, and new-core tests.
Run `cargo xtask full` for the legacy node/CLI suites as well. `cargo xtask arch` checks
the dependency direction without building runtime integrations.
