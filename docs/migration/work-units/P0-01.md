---
status: CURRENT
owner: migration/P0-01
source_of_truth: scripts/generate_current_workspace_inventory.py
last_verified_commit: 800df24076c7
verification: python3 scripts/generate_current_workspace_inventory.py --check
---

# P0-01: current inventory generation

Requirement: R01, R21, R22, R24  
Phase/work package: P0-01  
Current owner/path: repository-wide legacy manifests  
Target owner/path: root workspace and target hierarchy (Phase 1 onward)  
Inputs/accepted ADRs: no ADR is required to record current truth  
State authority affected: none; this work is read-only inventory generation  
Contract/schema change: adds generated formats for A001-A007 except behavioral fixtures (A008)  
Migration/cutover: none  
Rollback: remove the generator and its generated A001 outputs  
Security/threat impact: records dependency edges; makes no authorization change  
Performance/index/backpressure impact: none  
Tests and commands: `python3 scripts/generate_current_workspace_inventory.py --check`; `python3 scripts/generate_current_surface_inventories.py --check`; `python3 scripts/generate_legacy_data_inventory.py --check`; `python3 scripts/generate_current_call_graph.py --check`  
Generated docs/inventories: `docs/generated/current-workspace.*`, `docs/generated/current-routes.*`, `docs/generated/current-configuration.*`, `docs/generated/current-runtime-matrix.*`, `docs/generated/current-cli-ops.*`, `docs/generated/current-storage-access.json`, `docs/generated/current-call-graph.json`, `docs/migration/legacy-data-map.md`, `docs/migration/current-call-graph.md`  
Removal-ledger entries: tracked `dist/`, split Cargo roots, compile-time VM aggregator (to be expanded in A011)  
Evidence and known limitations: A001 covers manifests, direct dependency/feature declarations, targets, lockfiles, and tracked-file topology. A002 records registered shell actions, static HTTP routes, ingress patterns, and guest-operation aliases. A003 records literal configuration keys and deployment writers but computed keys/effective defaults need semantic review. A004 records physical RocksDB layouts, typed objects, direct/candidate key accesses, QuestDB tables, and the separate Hashgraph/OpenRaft stores; dynamic keys and payload schemas remain unresolved. A005 maps all registered shell actions to handlers, entry guards, direct transaction/service/VMM evidence, while explicitly flagging indirect helper effects. A006 records runtime metadata and distinguishes direct operation overrides from inherited defaults. A007 records CLI dispatch tables and root-script options. Characterization fixtures and accepted behavior semantics remain separate Phase 0 artifacts.
